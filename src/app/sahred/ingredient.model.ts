export class ingredient{
    // automatic arguments constructor
    // The public modifier in the constructor variables will automatically behind the scene :
    // create the variable in the class and make the assignement 
    constructor(public name : string, public amount : number){}
}